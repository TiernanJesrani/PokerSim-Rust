[package]
name = "poker_sim"
version = "0.1.0"
edition = "2021"

[dependencies]
polars = "0.46.0"
rand = "0.9.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "my_benchmark"
harness = false
